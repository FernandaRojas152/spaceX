import { FerDirective } from './fer.directive';

describe('FerDirective', () => {
  it('should create an instance', () => {
    const directive = new FerDirective();
    expect(directive).toBeTruthy();
  });
});
